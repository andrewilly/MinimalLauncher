<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to query all packages (needed for launcher to see installed apps) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- CRITICAL: On Android 11+ (API 30+), this tells the system we need to see
         all apps with launcher intents. Without this, queryIntentActivities returns
         an empty list and NO apps are shown. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinimalLauncher"
        tools:targetApi="34">

        <!-- ==================== MAIN LAUNCHER ACTIVITY ==================== -->
        <activity
            android:name=".ui.HomeActivity"
            android:clearTaskOnLaunch="true"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="stateAlwaysHidden|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ==================== APP DRAWER ACTIVITY ==================== -->
        <activity
            android:name=".ui.AppDrawerActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MinimalLauncher.Drawer"
            android:windowSoftInputMode="adjustResize" />

        <!-- ==================== SETTINGS ACTIVITY ==================== -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:theme="@style/Theme.MinimalLauncher.Settings" />

        <!-- ==================== HIDDEN APPS SETTINGS ==================== -->
        <activity
            android:name=".ui.HiddenAppsActivity"
            android:exported="false"
            android:label="@string/hidden_apps"
            android:theme="@style/Theme.MinimalLauncher.Settings" />

        <!-- ==================== HOME APPS MANAGEMENT ==================== -->
        <activity
            android:name=".ui.HomeAppsActivity"
            android:exported="false"
            android:label="App in home"
            android:theme="@style/Theme.MinimalLauncher.Settings" />

    </application>

</manifest>
